/** The hand-maintained datasets, re-exported for the prototypes and the page renderer. */
export { careers } from '../../data/career'
export { projects } from '../../data/projects'
export { blogPosts } from '../../data/blog'
export type { Career, Project, BlogPost, Image, Tag } from '../../types/data'
